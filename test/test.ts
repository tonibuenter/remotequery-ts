/* tslint:disable:no-unused-expression */
import { expect } from 'chai';

describe('Options tests', () => {
  // the tests container
  it('checking default options', () => {
    // the single test

    const a = false;

    /* detect retina */
    expect(a).to.be.false; // Do I need to explain anything? It's like writing in English!
  });
});
